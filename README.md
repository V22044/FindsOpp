
## Running the Application

### Bypassing Execution Policy (Windows)

If you encounter execution policy restrictions on Windows, use the following command:
```powershell
powershell -ExecutionPolicy Bypass
```

### Starting the Application

Then you can run the Application as normal:
```bash
npm start
```
